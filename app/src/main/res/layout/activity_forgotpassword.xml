<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.mysterious.filecrypt.Activities.ForgotPasswordActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:text="PUT ON HOLD !!"
        android:textStyle="bold"
        android:textSize="40sp"
        android:gravity="center"
        android:id="@+id/tvHold"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:text="Later on Functionality will be added to mail the Password to the registered Email-Id on Request"
        android:textStyle="italic"
        android:textSize="20dp"
        android:id="@+id/tvMessage"
        android:layout_below="@+id/tvHold"
        android:layout_marginTop="30dp"/>

</RelativeLayout>
